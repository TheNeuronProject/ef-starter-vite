{
  "name": "{{name}}",
  "version": "0.0.0",
  "description": "{{description}}",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "author": "{{contact}}",
  "license": "{{license}}",
  "dependencies": {
    "ef-core": "^0.13.4"
  },
  "devDependencies": {
    "eft-parser": "^0.13.3",
    "rollup-plugin-eft": "^0.13.4",
    "vite": "^2.0.0"
  }
}
